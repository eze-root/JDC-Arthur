# 改进记录

此文件记录 Codex 对仓库做出的每一次实质性修改。后续每个 PR 都必须在合并前
补充日期、改进内容、兼容性影响和验证结果。

## 2026-08-08

### 同步上游

- 将 `breeze303/openwrt-ci` 的 `main` 合并到本仓库，纳入共同祖先
  `fdd24c8` 之后的 26 个上游提交。
- 保留本仓库 77 个独有提交，以及 JDC-Arthur、Sing-box TProxy、默认网络配置
  和实验室环境相关定制。
- `configs/x86-64.config` 采用按 `CONFIG_*` 键的三方合并；真正冲突的配置以
  本仓库值为准，同时加入上游新增的软件包和平台选项。

### 文档与下载

- 将 GitHub Pages 部署切换到官方 Pages artifact 流程，并改为 `main` 分支文档
  变更自动部署。
- 新增“固件下载”页面，动态读取 Releases，提供编译产物的直接下载链接、文件
  大小、更新时间和下载次数。
- 新增维护指南与 PR 检查清单，要求每次修改同步更新本记录。

### 构建可靠性

- 为 JDC-Arthur 两个固件工作流显式授予 Release 写权限。
- 修复空 Docker 镜像列表可能导致初始化失败的问题。
- 改为在编译产物的 manifest 中读取实际内核版本，避免对目录执行 `grep`。
- 更新同名 Release 时替换旧产物，避免残留过期文件。

### 验证

- 上游三方合并完成且无未解决冲突。
- OpenWrt 配置文件无重复键或冲突标记。
- PR 会执行一次真实 Sphinx HTML 构建；合并后再由同一工作流部署 Pages。
- 工作流 YAML 通过语法解析。
